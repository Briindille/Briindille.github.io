var typingEffect = new Typed(".multiText", {
    strings : ["à la recherche d'un emploi", "dev d'applications"],
    loop: true,
    typeSpeed: 100,
    backSpeed: 80,
    backDelay: 1500,
});
